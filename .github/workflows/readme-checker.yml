name: Readme Checker

on:
  push:
    branches:
      - main


jobs:
  readme-checker:
    runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
          if: ${{ ! hashFiles('README.md') != '' }}
          run: exit 1