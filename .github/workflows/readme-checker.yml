name: Readme Checker

on:
  pull_request:
    branches:
      - main


jobs:
  readme-checker:
    runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
          if: ${{ ! hashFiles('README.md') != '' }}
          run: exit 0